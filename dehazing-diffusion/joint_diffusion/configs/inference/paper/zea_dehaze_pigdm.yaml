## Inference parameters
# path to data root
data_root: null
# dataset
run_name: zea_tissue
# checkpoints
run_id:
  sgm: null  # Fill with path to trained tissue model checkpoint
# device
device: 0
# random seed
seed: 1234
# number of images to process
num_img: 5
# keep track of images in each iteration and animate at the end
keep_track: true
# standard deviation of additive white gaussian noise (haze strength gamma)
noise_stddev: 0.5
# type of corruption
corruptor: haze
# whether to corrupt data and use as pairs or assume data is corrupted
paired_data: true
# plot zoomed in version as well (provide coordinates here as list, (x, y, size))
zoom: null

# sample from prior distribution for comparison
show_noise_priors: false
# disable corruptor model (use regular denoising)
disable_corruptor_model: false

limit_n_samples: 100

metrics:
  - psnr
  - ssim
models:
  - sgm

sgm:
  lambda_coeff: 0.5
  kappa_coeff: 0.5
  # number of noise scales (i.e. time steps in SDE)
  num_scales: 200
  sde: simple
  sigma: 25
  sampling_method: pc
  predictor: euler_maruyama
  corrector: none
  guidance: pigdm
  corruptor_run_id: null  # Fill with path to trained haze model checkpoint
  batch_size: 8
